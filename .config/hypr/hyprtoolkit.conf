# hyprtoolkit.conf — rose pine dark theme
# https://rosepinetheme.com/palette/

background = FF191724       # base (main background)
base = FF1F1D2E             # surface background
alternate_base = FF26233A   # slightly lighter background for alt elements
text = FFE0DEF4             # primary text color
bright_text = FFF5E0DC      # brighter accent text
accent = FFF6C177           # gold accent (rose-pine’s “gold”)
accent_secondary = FF9CCFD8 # blue accent (rose-pine’s “foam”)
# hyprtoolkit.conf — rose pine dark theme
# https://rosepinetheme.com/palette/

background = FF191724       # base (main background)
base = FF1F1D2E             # surface background
alternate_base = FF26233A   # slightly lighter background for alt elements
text = FFE0DEF4             # primary text color
bright_text = FFF5E0DC      # brighter accent text
accent = FFF6C177           # gold accent (rose-pine’s “gold”)
accent_secondary = FF9CCFD8 # blue accent (rose-pine’s “foam”)

icon_theme =
